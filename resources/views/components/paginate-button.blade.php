<div class="justify-content-center my-4 text-center">
    <a href="{{ $prev }}" class="btn btn-primary {{ $prev == '' ? 'disabled' : '' }}"
        {{ $prev == '' ? 'disabled' : '' }}>Previous</a>
    <a href="{{ $next }}" class="btn btn-primary {{ $next == '' ? 'disabled' : '' }}"
        {{ $next == '' ? 'disabled' : '' }}>Next</a>
</div>
